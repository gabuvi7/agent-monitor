# OpenSpec — agent-monitor
# Auto-generated by sdd-init (2026-06-04)
# Hybrid mode: OpenSpec + Engram artifacts

project: agent-monitor
engram_project: agent-monitor

stack:
  runtime: Node.js ESM
  server: node:http log server (`server.js`)
  client: vanilla HTML/CSS/JS (`public/`)
  observability: repo-owned OpenCode plugin source (`src/opencode/plugins/subagent-tracer.ts`) installed globally via `npm run install-plugin`
  local_prototype: project plugin (`.opencode/plugin/subagent-tracer.ts`)
  storage: filesystem logs under `logs/<project-hash>/`

packages:
  - root — single-package Node app (`package.json`)

strict_tdd: false
testing:
  runner: null
  commands:
    all: null
    unit: null
    lint: null
    typecheck: null
  layers:
    unit: null
    integration: null
    e2e: null
  coverage: unavailable (no test runner detected)
  quality:
    linter: null
    type_checker: null
    formatter: null

persistence: hybrid
workflow:
  execution_mode: interactive
  chained_pr_strategy: auto-forecast
  review_budget_lines: 800

context: |
  Stack: Node.js ESM server plus vanilla web UI.
  Architecture: the repo-owned OpenCode plugin source is installed globally and writes central NDJSON and timeline logs that this project serves locally.
  Runtime source of truth: `src/opencode/plugins/subagent-tracer.ts`; `~/.config/opencode/plugins/subagent-tracer.ts` is install output, not hand-edited source.
  Product direction: evolve toward an OpenCode-integrated TUI/statusline for subagent observability.
  Testing: no automated test runner, linter, type checker, formatter, or CI detected yet.

rules:
  proposal:
    - Lead with the user outcome and observability gap being closed.
    - State whether work targets the global plugin, the local viewer, or future TUI integration.
  specs:
    - Use RFC 2119 keywords and Given/When/Then scenarios.
    - Keep observability data contracts explicit: session, task, agent, timing, model, and token/context metadata.
  design:
    - Document event flow from OpenCode plugin capture to rendered UI or future TUI surfaces.
    - Call out failure modes when metadata is missing, delayed, or partial.
  tasks:
    - Group tasks by capture, normalization, presentation, and verification.
    - Keep each task independently reviewable within the configured budget.
  apply:
    - Follow existing Node ESM and no-build patterns unless the change justifies new tooling.
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Preserve archived changes as the observability decision trail.
